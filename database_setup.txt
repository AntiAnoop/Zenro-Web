
-- ----------------------------------------------------------------
-- ZENRO DATABASE SETUP SCRIPT (ROBUST V2)
-- Supports: Courses, Tests (w/ Assignments), Fees, Hybrid IDs
-- ----------------------------------------------------------------

-- 1. CLEANUP (Drop existing to ensure fresh state)
DROP TABLE IF EXISTS test_enrollments CASCADE;
DROP TABLE IF EXISTS test_batches CASCADE;
DROP TABLE IF EXISTS payments CASCADE;
DROP TABLE IF EXISTS fees CASCADE;
DROP TABLE IF EXISTS submissions CASCADE;
DROP TABLE IF EXISTS questions CASCADE;
DROP TABLE IF EXISTS tests CASCADE;
DROP TABLE IF EXISTS course_enrollments CASCADE;
DROP TABLE IF EXISTS course_batches CASCADE;
DROP TABLE IF EXISTS course_materials CASCADE;
DROP TABLE IF EXISTS course_modules CASCADE;
DROP TABLE IF EXISTS courses CASCADE;
DROP TABLE IF EXISTS batches CASCADE;
DROP TABLE IF EXISTS profiles CASCADE;

-- 2. CREATE TABLES

-- Profiles (Users)
CREATE TABLE profiles (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  email text UNIQUE NOT NULL,
  password text NOT NULL, -- In production, use Supabase Auth. This is for demo/prototype.
  full_name text NOT NULL,
  role text CHECK (role IN ('STUDENT', 'TEACHER', 'ADMIN')),
  batch text, -- Main batch assignment
  phone text,
  student_id text, -- Custom Roll Number
  avatar_url text,
  created_at timestamptz DEFAULT now()
);

-- Batches (Cohorts)
CREATE TABLE batches (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  name text UNIQUE NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- --- COURSE MANAGEMENT ---

CREATE TABLE courses (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  title text NOT NULL,
  description text,
  level text CHECK (level IN ('N5', 'N4', 'N3', 'N2', 'N1')),
  thumbnail text,
  status text DEFAULT 'DRAFT', -- DRAFT, PUBLISHED, ARCHIVED
  instructor_name text,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE course_modules (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  course_id text REFERENCES courses(id) ON DELETE CASCADE,
  title text NOT NULL,
  "order" int DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE course_materials (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  module_id text REFERENCES course_modules(id) ON DELETE CASCADE,
  title text NOT NULL,
  type text CHECK (type IN ('PDF', 'DOC', 'LINK', 'VIDEO')),
  url text NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- Course Visibility (Batches)
CREATE TABLE course_batches (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  course_id text REFERENCES courses(id) ON DELETE CASCADE,
  batch_name text NOT NULL, -- Links to batches.name loosely or strictly
  created_at timestamptz DEFAULT now()
);

-- Course Visibility (Individual Students)
CREATE TABLE course_enrollments (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  course_id text REFERENCES courses(id) ON DELETE CASCADE,
  student_id text REFERENCES profiles(id) ON DELETE CASCADE,
  created_at timestamptz DEFAULT now()
);

-- --- TEST & EXAM MANAGEMENT (UPDATED) ---

CREATE TABLE tests (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  title text NOT NULL,
  description text,
  duration_minutes int DEFAULT 30,
  passing_score int DEFAULT 40,
  is_active boolean DEFAULT false, -- Toggle for Draft/Published
  created_at timestamptz DEFAULT now()
);

CREATE TABLE questions (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  test_id text REFERENCES tests(id) ON DELETE CASCADE,
  question_text text NOT NULL,
  options text[] NOT NULL, -- Array of strings for options
  correct_option_index int NOT NULL,
  marks int DEFAULT 1,
  created_at timestamptz DEFAULT now()
);

-- Test Visibility (Batches) - NEW
CREATE TABLE test_batches (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  test_id text REFERENCES tests(id) ON DELETE CASCADE,
  batch_name text NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- Test Visibility (Individual Students) - NEW
CREATE TABLE test_enrollments (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  test_id text REFERENCES tests(id) ON DELETE CASCADE,
  student_id text REFERENCES profiles(id) ON DELETE CASCADE,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE submissions (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  test_id text REFERENCES tests(id) ON DELETE CASCADE,
  student_id text REFERENCES profiles(id) ON DELETE CASCADE,
  score int NOT NULL,
  total_score int NOT NULL,
  answers jsonb, -- Stores key-value of {question_id: selected_index}
  status text CHECK (status IN ('IN_PROGRESS', 'COMPLETED', 'TERMINATED')),
  feedback text,
  completed_at timestamptz DEFAULT now()
);

-- --- FINANCIALS ---

CREATE TABLE fees (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  student_id text REFERENCES profiles(id) ON DELETE CASCADE,
  title text NOT NULL,
  amount numeric NOT NULL,
  due_date date,
  status text DEFAULT 'PENDING' CHECK (status IN ('PENDING', 'PAID', 'OVERDUE')),
  category text, -- TUITION, EXAM, etc.
  phase int DEFAULT 1,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE payments (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  fee_id text REFERENCES fees(id) ON DELETE CASCADE,
  razorpay_payment_id text,
  amount numeric,
  payment_date timestamptz DEFAULT now()
);

-- 3. SEED DATA

-- Batches
INSERT INTO batches (name) VALUES ('2024-A'), ('2024-B'), ('2024-C');

-- Profiles
-- Teacher
INSERT INTO profiles (id, email, password, full_name, role, phone, student_id) 
VALUES ('t1', 'teacher@zenro.jp', '8888888888', 'Tanaka Sensei', 'TEACHER', '8888888888', 'EMP001');

-- Student
INSERT INTO profiles (id, email, password, full_name, role, batch, phone, student_id) 
VALUES ('s1', 'student@zenro.jp', '9999999999', 'Alex Student', 'STUDENT', '2024-A', '9999999999', '9999999999');

-- Admin
INSERT INTO profiles (id, email, password, full_name, role, phone, student_id) 
VALUES ('a1', 'admin@zenro.jp', '7777777777', 'Admin User', 'ADMIN', '7777777777', 'ADM001');

-- Sample Course
INSERT INTO courses (id, title, description, level, thumbnail, status, instructor_name) 
VALUES ('c1', 'JLPT N4 Grammar Mastery', 'Comprehensive guide to N4 grammar structures.', 'N4', 'https://images.unsplash.com/photo-1528164344705-47542687000d', 'PUBLISHED', 'Tanaka Sensei');

-- Assign Course to Batch
INSERT INTO course_batches (course_id, batch_name) VALUES ('c1', '2024-A');

-- Sample Test
INSERT INTO tests (id, title, description, duration_minutes, passing_score, is_active)
VALUES ('test1', 'N4 Weekly Mock Exam', 'A practice test for the upcoming JLPT.', 45, 50, true);

-- Assign Test to Batch
INSERT INTO test_batches (test_id, batch_name) VALUES ('test1', '2024-A');

-- Sample Questions
INSERT INTO questions (test_id, question_text, options, correct_option_index, marks)
VALUES 
('test1', 'Meaning of "Taberu" (食べる)?', ARRAY['To Drink', 'To Eat', 'To Sleep', 'To Run'], 1, 2),
('test1', 'Select the correct particle: Watashi __ Tanaka desu.', ARRAY['wa', 'ga', 'wo', 'ni'], 0, 1);
